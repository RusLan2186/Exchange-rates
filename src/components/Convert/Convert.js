import React from 'react';

const Convert = () => {
  const corrency = ['ua', 'usa', 'eur', [pln]];
  return (
    <div>
      <h1>Convert</h1>
      <button>convert</button>
    </div>
  );
};

export default Convert;
